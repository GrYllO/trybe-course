const multiplyByThree = require('./foreach');

describe('The function multiplyByThree', () => {
  it('is a function', () => {
    expect(typeof multiplyByThree).toBe('function');
  });

  it('', () => {});
});